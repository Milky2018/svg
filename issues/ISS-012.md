# ISS-012: Expose custom-property resolution for extension values

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: css, custom-properties, upstream, mizchi-css
- Assignee: codex
- Created: 2026-07-20
- Updated: 2026-07-22
- External ref: https://github.com/mizchi/css/issues/11

## Description
`mizchi/css` computes inherited custom-property maps and resolves `var()` for properties known to its computed-style engine. SVG-specific extension properties such as `fill`, `stroke`, marker references, and SVG lengths also need the same resolver, but the upstream generic resolver is private.

## Design
Request or add a public upstream API that resolves a cascaded extension-property value against `ComputeContext`, including nested fallbacks and invalid cycles. This repository should call that API and retain only SVG-specific value parsing.

## Acceptance Criteria
- [x] `Milky2018/css` exposes a generic custom-property value resolver for extension values.
- [x] Nested fallback, missing value, empty values, and cycle behavior are covered in the CSS module.
- [x] SVG consumes the workspace API without waiting for an external release.
- [x] The SVG-local generic `var()` scanner is deleted.

## Relationships
- Depends on: none
- Parent: ISS-003
- Related: ISS-005, ISS-010, ISS-011
- Discovered from: ISS-010

## Notes
- 2026-07-20: The integration now obtains inherited custom-property maps from `compute_style_with_vars`; only expansion inside SVG-specific values still uses the compatibility scanner.
- 2026-07-20: Reported upstream as mizchi/css#11.
- 2026-07-22: Unblocked after the CSS core became the locally maintained `Milky2018/css` workspace module; implementation no longer waits for an upstream release.

## Close Notes

Closed 2026-07-22. Added `resolve_custom_property_value` to the CSS computed package and facade, covered nested and inherited substitutions, missing values, empty values, fallbacks, and cycles, and removed the duplicate SVG resolver. Verified by four-backend 1750/1750 workspace tests and strict all-target checks.
