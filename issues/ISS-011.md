# ISS-011: Complete CSS Color 3 parsing

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: css, color, upstream, mizchi-css
- Assignee: codex
- Created: 2026-07-20
- Updated: 2026-07-22
- External ref: https://github.com/mizchi/css/issues/10

## Description
`mizchi/css` 0.7.3 owns general CSS color parsing, but its computed color parser does not resolve HSL/HSLA and maps unsupported or invalid colors to transparent. SVG needs HSL/HSLA and a result that distinguishes invalid syntax from a valid transparent color before the local compatibility parser can be removed.

## Design
Fix this in `mizchi/css`, not by adding another color grammar or keyword table to this repository. After an upstream release exposes the required behavior, adapt its typed color result to SVG `Color` and delete the local general color parser.

## Acceptance Criteria
- [x] `Milky2018/css` parses HSL and HSLA from CSS Color 3.
- [x] The CSS module distinguishes invalid color syntax from valid transparent.
- [x] SVG consumes the local workspace behavior without an external release.
- [x] SVG uses the CSS result for solid colors and removes its general color parser.

## Relationships
- Depends on: none
- Parent: ISS-005
- Related: ISS-010, ISS-012
- Discovered from: ISS-010

## Notes
- 2026-07-20: Directly switching computed `color` to mizchi/css made the existing HSL/currentColor regression test fail on all four backends.
- 2026-07-20: The upstream color test explicitly accepts that HSL may not resolve; this is an upstream capability gap rather than an SVG-domain concern.
- 2026-07-20: Reported upstream as mizchi/css#10.
- 2026-07-22: Unblocked after vendoring the CSS core as `Milky2018/css`.

## Close Notes

Closed 2026-07-22. Added HSL/HSLA, strict RGB and hex validation, and an explicit `Invalid` color result while retaining named colors and transparent. SVG now only adapts typed CSS colors to renderer colors.
