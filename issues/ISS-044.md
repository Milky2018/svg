# ISS-044: Prune the CSS implementation to its accepted scope

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: api, modules/css, css, cleanup, migration, agent
- Assignee: codex
- Created: 2026-07-22
- Updated: 2026-07-22
- External ref: none

## Description
Implement the CSS module boundary accepted in ISS-043. Remove, internalize, or relocate copied packages and public APIs that the repository does not intend to maintain while keeping the static cascade and computed semantics required by SVG.

## Design
Use package and generated-interface analysis to remove complete unused capability slices rather than leaving half-supported facade exports. Preserve selector matching, declaration parsing and recovery, cascade ordering, media evaluation, CSS-wide keywords, inheritance, custom-property resolution, color parsing, and computed values where required by SVG. Extract narrowly shared color-interpolation helpers before removing animation runtime code if necessary.

## Acceptance Criteria
- [x] Every disposition accepted in ISS-043 is implemented.
- [x] The CSS facade exposes only intentionally supported types and functions.
- [x] SVG contains no replacement copy of retained CSS algorithms.
- [x] CSS unit tests cover the retained generic contracts and SVG integration tests cover the adapter.
- [x] `moon info`, formatting, all-target deny-warn checks, and tests pass.

## Relationships
- Depends on: ISS-043
- Parent: ISS-036
- Related: ISS-039, ISS-045, ISS-046
- Discovered from: ISS-036

## Notes
- 2026-07-22: Do not treat code as removable solely because the SVG module does not call a public API directly; ISS-043 owns that product decision.

## Close Notes

- 2026-07-22: Removed the animation effect/sampling package, the computed
  keyframe-to-effect bridge, and the generic layout-support diagnostics package.
  Static animation/transition declarations and `@keyframes` parsing remain.
- 2026-07-22: Moved the color-space interpolation required by `color-mix()`
  into the computed-value package and kept the existing CSS Color tests.
- 2026-07-22: Regenerated package interfaces; `moon fmt --check`, all-target
  deny-warn checks, all 1,539 tests on each backend, and `git diff --check`
  pass.
